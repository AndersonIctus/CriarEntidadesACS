package main.geradores.model.utils;

public enum PropertyType {
    NUMERO,
    STRING,
    CHAR,
    TEXT,
    SHORT,
    LONG,
    DECIMAL,
    DATE,
    TIMESTAMP,
    ACS_DATE_TIME,
    JOIN,
    JOIN_COMPOSTO,
    JOIN_COMPOSTO_CHAVE,
    AUDITORIA
}
